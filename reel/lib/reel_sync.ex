defmodule ReelSync do
  @moduledoc """
  ReelSync is responsible for syncing the TMDB dataset
  to our local sqlite reel_sync.db database.

  It also has functionality that writes a more stripped down
  version of the dataset to reel.db. This dataset is what
  gets deployed to our production app.
  """
end
